[package]
name = "agent-store-fs"
version = "0.1.0"
edition = "2021"
description = "Filesystem-backed TaskStore for agent-core — durable task history/status."
license = "MIT"

[dependencies]
agent-core = { path = "../agent-core" }
async-trait = "0.1"
serde_json = "1"

[dev-dependencies]
pollster = "0.3"
