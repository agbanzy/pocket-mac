// Objective-C bridging header: exposes the shared Rust agent's C ABI to Swift.
// The library itself is built by the "Build Rust agent" pre-build phase (see mac/project.yml).
#import "pocketmac_agent.h"
